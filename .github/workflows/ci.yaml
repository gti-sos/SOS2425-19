name: CI
on: [push,pull_request]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Install dependences
        run: npm install
      - name: Run tests
        run: npm test